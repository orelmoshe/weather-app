import _ from 'lodash';

// export const incubatorInfoAdapter = (current): IncubatorInfoInterface => {
// 	return {
// 		incubator: incubatorAdapter(current.Incubator),
// 		trolleys: _.map(current.TrolleysVm, t => trolleyVmAdapter(t)),
// 	};
// };
